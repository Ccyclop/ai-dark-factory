# secrets and local config — never commit
.env
.env.*
*.pem
*.key
.claude/settings.local.json

# per-clone OpenCode seat files (created by factory/setup.sh)
/opencode.json
/AGENTS.md

# OS noise
.DS_Store
